/* eslint-disable new-cap */
import React from "react";
import Radium from "radium";

const Typography = (props) => {
  return (
    <div>
      <p>Nothing to see here</p>
    </div>
  );
};

export default Radium(Typography);
